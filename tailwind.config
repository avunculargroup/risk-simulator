import type { Config } from "tailwindcss";

const config: Config = {
  content: ["./src/**/*.{ts,tsx}"],
  darkMode: "class", // toggled via .dark on <html>, not system preference
  theme: {
    extend: {
      // ─────────────────────────────────────────────────
      // COLOURS — reference CSS custom properties defined
      // in globals.css so light/dark mode works via class toggle.
      //
      // Usage: bg-bts-surface, text-bts-primary, border-bts-border, etc.
      //
      // See DESIGN_BRIEF.md for the full colour specification.
      // ─────────────────────────────────────────────────
      colors: {
        bts: {
          bg: "var(--color-bg)",
          surface: "var(--color-surface)",
          "surface-subtle": "var(--color-surface-subtle)",
          border: "var(--color-border)",
          "border-light": "var(--color-border-light)",

          // Text
          primary: "var(--color-text-primary)",
          secondary: "var(--color-text-secondary)",
          tertiary: "var(--color-text-tertiary)",

          // Gold accent (derived from logo gradient)
          gold: "var(--color-gold)",
          "gold-light": "var(--color-gold-light)",
          "gold-dark": "var(--color-gold-dark)",
          "gold-glow": "var(--color-gold-glow)",

          // Semantic — fixed across light/dark modes
          gain: "var(--color-gain)",
          "gain-dim": "var(--color-gain-dim)",
          loss: "var(--color-loss)",
          "loss-dim": "var(--color-loss-dim)",
          warning: "var(--color-warning)",
          "warning-dim": "var(--color-warning-dim)",
          info: "var(--color-info)",
          "info-dim": "var(--color-info-dim)",
        },
      },

      // ─────────────────────────────────────────────────
      // TYPOGRAPHY — Playfair Display for headings,
      // DM Sans for body/UI, JetBrains Mono for data.
      // ─────────────────────────────────────────────────
      fontFamily: {
        display: ["'Playfair Display'", "Georgia", "serif"],
        sans: ["'DM Sans'", "system-ui", "sans-serif"],
        mono: ["'JetBrains Mono'", "'Fira Code'", "monospace"],
      },

      fontSize: {
        "display": ["3rem", { lineHeight: "1.1", fontWeight: "700" }],
        "page-title": ["1.75rem", { lineHeight: "1.25", fontWeight: "600" }],
        "section-title": ["1.25rem", { lineHeight: "1.35", fontWeight: "600" }],
        "subsection": ["1rem", { lineHeight: "1.4", fontWeight: "600" }],
        "body": ["0.9375rem", { lineHeight: "1.6", fontWeight: "400" }],
        "body-sm": ["0.8125rem", { lineHeight: "1.5", fontWeight: "400" }],
        "caption": ["0.6875rem", { lineHeight: "1.3", fontWeight: "600", letterSpacing: "0.04em" }],
        "data-lg": ["1.625rem", { lineHeight: "1.2", fontWeight: "700" }],
        "data-md": ["0.8125rem", { lineHeight: "1.4", fontWeight: "600" }],
        "data-sm": ["0.6875rem", { lineHeight: "1.3", fontWeight: "500" }],
      },

      // ─────────────────────────────────────────────────
      // SPACING & LAYOUT
      // ─────────────────────────────────────────────────
      maxWidth: {
        "dashboard": "1400px",
        "content": "1200px",
      },
      width: {
        "sidebar": "260px",
      },

      // ─────────────────────────────────────────────────
      // BORDER RADIUS — from design brief
      // ─────────────────────────────────────────────────
      borderRadius: {
        "sm": "4px",
        "md": "6px",
        "lg": "8px",
        "xl": "12px",
      },

      // ─────────────────────────────────────────────────
      // SHADOWS — warm-tinted, CSS vars for mode switching
      // ─────────────────────────────────────────────────
      boxShadow: {
        "bts-sm": "var(--shadow-sm)",
        "bts-md": "var(--shadow-md)",
        "bts-lg": "var(--shadow-lg)",
      },

      // ─────────────────────────────────────────────────
      // ANIMATION — precise, not playful
      // ─────────────────────────────────────────────────
      animation: {
        "fade-in": "fadeIn 150ms ease",
        "slide-up": "slideUp 300ms cubic-bezier(0.32, 0.72, 0, 1)",
        "pulse-logo": "pulseLogo 1.5s ease-in-out infinite",
      },
      keyframes: {
        fadeIn: {
          from: { opacity: "0" },
          to: { opacity: "1" },
        },
        slideUp: {
          from: { transform: "translateY(100%)" },
          to: { transform: "translateY(0)" },
        },
        pulseLogo: {
          "0%, 100%": { opacity: "0.4" },
          "50%": { opacity: "1" },
        },
      },

      transitionDuration: {
        "fast": "100ms",
        "base": "150ms",
        "slow": "200ms",
        "sheet": "300ms",
      },
    },
  },
  plugins: [],
};

export default config;
